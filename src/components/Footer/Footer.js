import "./Footer.css";

const Footer = ({ className }) => {
  return (
    <footer className={`footer ${className}`}>
      &copy; 2022 Hikers. All Right Reserved.{" "}
    </footer>
  );
};

export default Footer;
